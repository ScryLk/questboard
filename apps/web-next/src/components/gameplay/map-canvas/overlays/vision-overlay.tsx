"use client";

// Vision overlay temporarily disabled — will be reimplemented as on-select only.

interface VisionOverlayProps {
  scaledCell: number;
  canvasW: number;
  canvasH: number;
  dragTokenId?: string | null;
  dragX?: number;
  dragY?: number;
}

export function VisionOverlay(_props: VisionOverlayProps) {
  return null;
}
